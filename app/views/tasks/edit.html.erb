<h1>id:<%= @task.id %>の編集ページ</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '入力' %>
<% end %>

<%= link_to 'タスク一覧を表示', tasks_path %>